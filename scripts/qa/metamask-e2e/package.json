{
  "name": "qa-metamask-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx run-source-regressions.ts --filter studio && tsx run-source-regressions.ts --filter cli && tsx run-source-regressions.ts --filter contracts",
    "test:e2e": "tsx run.ts",
    "regressions": "tsx run-source-regressions.ts",
    "regressions:studio": "tsx run-source-regressions.ts --filter studio",
    "regressions:studio-live": "tsx run-source-regressions.ts --filter studio-live",
    "regressions:cli": "tsx run-source-regressions.ts --filter cli",
    "regressions:contracts": "tsx run-source-regressions.ts --filter contracts"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "ethers": "^6.13.0",
    "dotenv": "^16.4.0",
    "@ivaronix/og-chain": "workspace:*",
    "@ivaronix/skills": "workspace:*"
  }
}
