# Secrets
.env
.env.*
!.env.example
!.env.*.template
da.env
*.key
*.pem
keystore/

# Claude Code internal state · everything under .claude/ EXCEPT path-scoped rules.
# Path-scoped agent rules ARE committed (planning-003 §A.4.2 · AlphaDawg pattern).
# Pattern: ignore .claude/* (file or dir directly under .claude/), then re-include the rules subdir.
.claude/*
!.claude/rules/
CLAUDE.md.local

# Internal asset-generation tooling (X/Twitter card images for launch
# marketing). The generator script + its non-repo output path are
# ignored; the published card images under x-post-assets/ stay tracked
# since they ship as actual launch artefacts.
scripts/qa/metamask-e2e/create-hype-posts.ts

# Reference / research material — kept locally only; not pushed
# (each contains nested .git/ folders, so they'd become gitlinks if tracked)
CLI Open Source Project/
oglabs resources/
og-projects-showcase/
entries/
new-entries/

# Internal planning + scratch docs — kept locally only; not pushed.
# These are working notes / scratch / internal QA tracking. Public-facing
# replacements live in README.md / CONTRIBUTING.md / CHANGELOG.md.
wanderingthoughts.md
wanderingflow.md
Ivaronix.md
Ivaronix_User_QA_Test_Plan.md
FINAL_BUILD_PLAN.md
Going_Extra.md
QA_TEST_PROGRESS.md
QA_FIX_LOG.md
SESSION_FINAL.md
IVARONIX_PAYMENT_STRATEGY.md
cowork-opinion.md
_archive/

# Internal QA evidence pack — screenshots, videos, raw logs. The
# external-facing proof story lives at: chainscan tx hashes in README,
# the `/r/<id>` proof pages, the CLI `receipt verify --tee-independent`
# command. Keep QA_PROOF_PACK/ locally for personal reference; not pushed.
QA_PROOF_PACK/

# Bundled visual mockup at brand/Ivaronix.html — pushed as design source of truth.


# Node / pnpm / Turbo
node_modules/
.turbo/
dist/
build/
*.tsbuildinfo
.next/
.nuxt/
.vercel/

# `build/` (above) is intentionally ignored at any depth as build-output dirs.
# Exception: docs/build/ holds the build-progress + engineering log narratives
# (BUILD_PROGRESS.md, ENGINEERING_DEBUG_LOG.md, TEST_REPORT.md, BUILD.md).
# Those files ARE tracked — without this re-include, edits to them silently
# don't stage. (Found in cron-sweep 2026-05-10 when canonicalizing the
# deployments path across operator runbooks.)
!docs/build/
!docs/build/**

# Foundry
out/
cache/
broadcast/
contracts/out/
contracts/cache/
contracts/broadcast/

# Local test artifacts
tmp/
*.tmp.json

# Local data
.ivaronix/
*.db
*.db-journal
*.sqlite
*.sqlite-journal

# OS
.DS_Store
Thumbs.db
*.log
*.tmp

# IDE
.vscode/
.idea/
*.swp
*.swo

# Test artifacts
coverage/
.nyc_output/
playwright-report/
.playwright-mcp/

# Visual evidence — kept locally, organized by round / surface; not pushed.
# Exception: screenshots/readme/ holds the 6 README "Visual tour" PNGs
# committed for GitHub render (planning-003 §A.2.2). Refresh via
# `pnpm screenshots:refresh` against a live Studio dev server.
# (Use `screenshots/*` not `screenshots/` so the unignore on the
# subdirectory below actually takes effect — git won't traverse into a
# fully-ignored parent dir.)
screenshots/*
!screenshots/readme/

# MetaMask Playwright profile dirs (runtime artefacts, not code)
scripts/qa/metamask-e2e/mm/

# Python cache from scripts/verifier-py/
scripts/verifier-py/__pycache__/

# Test target scratch files (operator's local docs / receipts / etc.)
test-targets/*
# Exception: dd-batch/ holds the 3 vendor-agreement fixtures for the
# `ivaronix doc bulk` flow (planning-01 §4B · DD-analyst batch audit).
# Tracked, not ignored. The pre-sweep-85 gitignore was a single
# `test-targets/` line that contradicted git ls-files (which had the
# 3 dd-batch/ fixtures tracked anyway).
!test-targets/dd-batch/
!test-targets/dd-batch/**

# _archive/ holds pre-v2 planning docs (kept for history; do not edit).
# Tracked, not ignored — the README docs grid + planning-003 §A.5.X
# both reference these files. Earlier `_archive/` entry in this file
# was self-contradicting drift; removed.

.vercel
cowork-opinion.md

# P3 ephemeral profile dirs (one per test run; safe to delete)
scripts/qa/ui-test-plan/.p3-profile-*
.env.mainnet

# ─── Internal-only documentation (kept locally for engineering) ──────────────
# These are operational, sprint-tracking, or design docs that don't belong in
# the public product surface. README/SECURITY/CONTRIBUTING/CHANGELOG/LICENSE
# + technical specs under docs/ that judges may want are tracked separately.
CLAUDE.md
COMPONENTS.md
HLD.md
JUDGE_REPORT.md
PRD.md
RECEIPTS_SPEC.md
UI_UX_GUIDE.md
docs/JUDGE_GUIDE.md
docs/JUDGE_REPLAY.md
docs/PITCH.md
docs/BUILD_COMPLETE_AUDIT.md
docs/CI_WALLET.md
docs/EXTRATESTING.md
docs/FINAL_BUILD_TEST_PLAN.md
docs/HALF_BAKED.md
docs/LOOP_DIRECTIVE.md
docs/MAINNET_PERFECT_PLAN.md
docs/MAINNET_PROMOTION_PLAN.md
docs/MULTI_WALLET_RULES.md
docs/PHASE_B_DISCLOSURES.md
docs/PRODUCT_FLOW.md
docs/QA_FULL_PRODUCT_REPORT.md
docs/QA_LOOP_BRIEF.md
docs/QA_MISSION.md
docs/QUALITY.md
docs/TESTNET_BUILD_PUNCH_LIST.md
docs/TOOL_LOOP_RUNTIME_EXTENSION.md
docs/UI_BEST_VERSION_AUDIT.md
docs/UI_HALF_BAKED_AUDIT.md
docs/UI_REAL_USER_TEST_PLAN.md
docs/USER_TODO.md
docs/PASS77_*.md
docs/PLAN_pass*.md
docs/planning-*.md
.claude/
