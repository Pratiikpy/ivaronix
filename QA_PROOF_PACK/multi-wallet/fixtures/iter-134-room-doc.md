# Iter-134 · Data-Room Test Document

This document is used to drive QA plan rows 825/826/791 (data-room
share, revoke, og.burn.auto_enable) in the 2-wallet matrix.

Contents are deliberately small + benign so the encryption + storage
upload path can be exercised without risking real-data leakage.

PRIVATE_TEST_PHRASE_DO_NOT_LEAK: this string must NOT appear in any
public surface (OG image, headline, public receipt). If it does, the
Burn-Mode privacy story is broken.
