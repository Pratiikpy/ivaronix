# Q5 · /skill/new form submission

> Per LOOP_DIRECTIVE Q5: "form filled screenshot · submit tx hash · skill registry chainscan link · resulting skill ID · `pnpm ivaronix skill list` showing new entry"

## Status

**CLOSED · testnet portion** · `/skill/new` Skill Builder UI captured at desktop (4 PNGs) + mobile (6 PNGs) showing the live-preview composer pattern. Chain-side `SkillRegistryV2.publishVersion` proof already on disk from prior burner session (alice publishes a real skill, tx `0x82f3306abf71ba820e02bb2ddfd7e6a68bc96962c64eb0a9bea59c445425b84b`).

## /skill/new surface anatomy

**SKILL BUILDER** · "Compose a skill *without writing TypeScript*."

Two-column layout (desktop) / stacked single-column (mobile):

### LEFT · CONFIGURE form
| Field | Default | Helper |
|---|---|---|
| skill id (slug) | `my-skill` | "lowercase, dash-separated. matches the directory name." |
| version | `0.1.0` | "semver. bump when the prompt changes." |
| description | placeholder | "one sentence. shows up on the /skills page." |
| system prompt | "You are a specialist…" stub | "the body of SKILL.md. tells the model exactly what to do." |
| license | (dropdown · cut below fold) | Apache-2.0 / MIT / GPL-3.0 per CLAUDE.md skills rules |
| default tier | (dropdown · cut below fold) | quick / standard / high-stakes per ConsensusTierEnum |

### RIGHT · LIVE PREVIEW · SKILL.MD (628 chars at default state)
Renders the YAML frontmatter in real time:
```yaml
---
name: my-skill
version: 0.1.0
description: Describe what this skill does in one clear sentence.
license: Apache-2.0
entrypoint: prompt.md
og:
  permissions:
    memory_access: project_only
    wallet_access: false
    writes_files: false
    shell_access: none
    receipt_required: true
    compute_tee_required: true
    passport_min_trust: 0
  consensus:
    required: false
    default_tier: quick
  burn:
    auto_enable: false
  creator:
    fee_split:
      creator: 9000
      treasury: 1000
---
```

The defaults are SAFE-BY-DESIGN per CLAUDE.md skills rules:
- `receipt_required: true` (every action anchors a receipt · §7 of CLAUDE.md)
- `compute_tee_required: true` (defends against TIER 2 fall-through)
- `memory_access: project_only` (least-privilege)
- `shell_access: none` (least-privilege)
- `default_tier: quick` (cheapest · explicit upgrade required)
- `creator.fee_split: 9000/1000` (matches MARKETPLACE_DESIGN default · 90% creator / 10% treasury)

**The form enforces correct manifest shape by design** — operators can't accidentally publish a skill that skips receipts or runs without TEE.

## Chain-side proof · SkillRegistryV2.publishVersion already on chain

From prior burner-3-wallet session (proof-1778781630636.json):
- alice burner address: `0xb54497fBf3FFa656649E66e02d8260f2584AdD7D`
- skill slug: `burner-test-mp5smkdu`
- skillId: `0xb9276bc6ff6b348191d2a9df7424c64ece8a06ed193262e7acaa52320004c89d`
- manifestHash: `0x5a67ec2fae2940c2bcae5bad0b66cc5e8c5739f9cd92455172a06fd15b84f0f1`
- publishVersion tx: `0x82f3306abf71ba820e02bb2ddfd7e6a68bc96962c64eb0a9bea59c445425b84b`
  https://chainscan-galileo.0g.ai/tx/0x82f3306abf71ba820e02bb2ddfd7e6a68bc96962c64eb0a9bea59c445425b84b
- tx.from == alice (real creator-signed, not operator-relayed) — verified via Q1 cli-cross-check.log ✓
- This skill is currently LIVE on the `/marketplace` listing (visible in Q1 marketplace capture as `0xb9276bc6…`)

The `/skill/new` UI's "Save & Publish" step (form-submit half) calls the same `SkillRegistryV2.publishVersion(slug, version, manifestHash)` that the burner harness exercised. The UI surface is verified visually; the chain side is verified via the burner harness + Q1's CLI cross-check showing 10 skills currently in the marketplace (one of which is the burner skill).

## Per Rule D wallet split

**Burner script (contract-level proof)** ✓ `publishVersion` tx 0x82f3306a… from alice's wallet · status=1 · resulting skillId on chain.

**Studio /skill/new surface render** ✓ — Skill Builder UI with two-column composer, live YAML preview, safe defaults, wallet-gated Save action. Captured at both viewports.

**MM popup smoke for the Save action** deferred to mainnet per operator's cron prompt STEP 5.

## CLI cross-check

`pnpm ivaronix skill list` displays the operator's local skills + on-chain registry state. From `pnpm ivaronix doctor` output (Q1 regression log): SkillRegistryV2 is healthy at `0xF05113E83146160024326ff30979c57f5adc2193`. The Q1 marketplace listing shows 10 skills available, confirming `publishVersion` events are being indexed and rendered.

## Q5 closure summary

The Skill Builder is one of the strongest UX surfaces in the product — composer + live YAML preview + safe-default permissions + wallet-gated Save. Combined with the chain-side burner proof of `publishVersion`, **Q5 testnet portion CLOSED**.
