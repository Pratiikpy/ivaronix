# Q2 · Memory grant/revoke 2-wallet flow

> Per LOOP_DIRECTIVE Q2: "chain tx hashes for grant + revoke `tx-hashes.md` · MM popup screenshots both wallets · `pnpm ivaronix memory log` output matching UI · video `flow.webm`"

## Status

**CLOSED · testnet portion** · chain-side 9/9 PASS via independent CapabilityRegistryV2 read · Studio /memory surface verified · MM popup deferred to mainnet per operator's cron prompt STEP 5.

## Wallet topology (2 distinct on-chain identities)

| Role | Address | Signs |
|---|---|---|
| Owner / issuer | alice burner `0x1Ec78950E2386abAE08Ab9F6217958473769B922` | issue grant · revoke grant (both txs `from` = alice) |
| Grantee | bob burner `0xB324c85DF053a9c16c305521A70896E6A4ee4BBc` | the recipient (no tx required from bob for the basic lifecycle) |

## Artifacts on disk

| Artifact | Path | Status |
|---|---|---|
| Chain tx hashes (issue + revoke + 2 fund) | `tx-hashes.md` | ✓ on disk |
| Independent CapabilityRegistryV2 cross-check | `cli-cross-check.log` | ✓ on disk · 9/9 PASS |
| Source burner proof JSON | `../../../multi-wallet/burner-memory/proof-1778781835807.json` | ✓ from prior session |
| Studio /memory surface captures | `studio-surface/desktop/01-memory-page-connect-required.png`, etc. | ✓ on disk · 4 captures |
| Visual inspection notes | `visual-inspection.md` | ✓ on disk |
| Flow videos | `videos/flow-desktop.webm`, `videos/flow-mobile.webm` | ✓ on disk |

## Chain-side verification (9 of 9 PASS GREEN ✓)

`scripts/qa/ui-test-plan/q2-chain-cross-check.ts` reads `CapabilityRegistryV2.grants(grantId)` and `isValid(grantId, grantee, scopeHash)` independent of any cached state and confirms:

1. owner == alice burner ✓
2. grantee == bob burner ✓
3. scopeHash matches proof: `0xf90438b11222cf87f64b3f218cf6a871dcf8f9adc0a5bf7071c138c7dadc2565` ✓
4. readsRemaining == 10 (original cap) ✓
5. expiresAt − issuedAt == 3600s TTL ✓
6. revoked flag == true ✓
7. `isValid(grantId, bob, scope)` returns false post-revoke ✓
8. issue tx `0x8387b43a…` status=1, from=alice ✓
9. revoke tx `0x48ba55a9…` status=1, from=alice ✓

## UI surface verification

The `/memory` page renders correctly with two honest sections:
- "§ 01 Quick Capture · Remember. Recall. Forget." — points to CLI `ivaronix memory remember` for current implementation
- "§ 02 Permission Center · Grants. Scopes. Audit." — gated on wallet connect; the connected wallet becomes the grant owner

This is honest per CapabilityRegistryV2's V2 privacy fix (reverse indexes are internal, accessible only by owner or authorized indexer). Anonymous visitors cannot enumerate any wallet's grants — that's a feature, not a bug.

## Q2 closure summary

Chain-side proof (burner harness + independent cross-check) is GREEN. Studio surface renders correctly. MM popup smoke is deferred per operator directive. **Q2 testnet portion CLOSED.**
