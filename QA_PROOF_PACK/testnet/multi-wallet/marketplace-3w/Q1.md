# Q1 · Marketplace 3-wallet flow (creator + buyer + treasury)

> Per LOOP_DIRECTIVE.md Q1 row: "3 distinct chainscan tx URLs in `tx-hashes.md` · 3 per-wallet MM popup screenshots · video of full flow `flow.webm` · CLI cross-check log showing fee-split distributed correctly."

## Status

**Chain-side: GREEN** · burner script proof + independent CLI cross-check both confirm the 3-wallet flow worked end-to-end with real on-chain side effects.  
**UI-side MM popup smoke: queued for next fire** · the Playwright + MM extension harness (`scripts/qa/metamask-e2e/`) is the proven path (CLAUDE.md §16.1 Add-Account derivation pattern) but requires a dedicated 2-3 minute run.

## Artifacts on disk

| Artifact | Path | Status |
|---|---|---|
| 3 distinct chainscan tx URLs (creator + buyer + treasury) | `tx-hashes.md` | ✓ on disk |
| Independent on-chain CLI cross-check (5/5 PASS) | `cli-cross-check.log` | ✓ on disk |
| Source burner proof JSON | `../../../multi-wallet/burner-3-wallet/proof-1778781630636.json` | ✓ on disk (prior session) |
| 3 per-wallet MM popup screenshots | `mm-popups/` | ⏳ next fire (Playwright + MM harness run) |
| Full flow video (`flow.webm`) | `videos/flow.webm` | ⏳ next fire (recordVideo) |

## Wallet topology (3 distinct on-chain identities, per Rule D)

| Role | Address | Signs which contract calls |
|---|---|---|
| Creator | `0xb54497fBf3FFa656649E66e02d8260f2584AdD7D` | `publishVersion` · `setPrice` · `withdrawCreator` |
| Buyer | `0x1cfF4eF4b114860F01C90126Db6AF7E71B50b713` | `paySkillRun` (0.005 OG value) |
| Treasury / admin | `0xaa954c33810029a3eFb0bf755FEF17863E8677Ce` | funding · admin actions |

The CLI cross-check verifies that the on-chain `tx.from` for `paySkillRun` is bob's wallet (not the operator relay) and `setPrice` is from alice's wallet — proving the 3 wallets are distinct signers, not a single wallet wearing multiple hats.

## CLI cross-check summary (5 of 5 PASS · GREEN ✓)

Captured at `cli-cross-check.log`:
- `SkillPricing.getPricing(skillId)` returns price=0.005 OG · creatorBps=9000 · treasuryBps=1000 · priced=true
- `SkillRunPayment.creatorBalance(alice)` = 0 OG (full withdraw to wallet)
- `SkillRunPayment.creatorLifetimeEarned(alice)` = 0.0045 OG (monotonic record of the one paid run)
- `SkillRunPayment.treasuryBalance()` = 0.008 OG (shared · non-zero · accumulated across all marketplace tests)
- `SkillRunPayment.treasuryLifetimeEarned()` = 0.018 OG (shared monotonic)
- `paySkillRun` tx.from = bob's wallet (status=1)
- `setPrice` tx.from = alice's wallet (status=1)

## Fee-split verified at event + state level

- Creator received exactly 0.9 × price (4500000000000000 wei) — 9000 bps ✓
- Treasury share of 0.0005 OG per tx accumulates in shared `treasuryBalance` — 1000 bps ✓
- bps observed in proof JSON (9000/1000) matches the bps stored in SkillPricing on chain ✓
- Alice withdrew her share — `creatorBalance[alice]` zeroed; `creatorLifetimeEarned[alice]` stayed monotonic at 0.0045 OG ✓
- Total invariant holds: `creatorShare + treasuryShare == amount` (4.5e15 + 0.5e15 == 5e15 wei) ✓

## What MM popup smoke would add (Rule D additive proof)

- Pixel-level proof that Studio's `/marketplace/[skillId]` page renders the Buy & Run button with correct copy ("Buy & run · 0.005 OG")
- Pixel-level proof that clicking Buy triggers a real MetaMask popup showing: correct `to` (SkillRunPayment 0x9eA5...50A5C), correct `value` (5000000000000000 wei = 0.005 OG), correct gas estimate
- Pixel-level proof that confirmed tx returns user to the receipt page with the four-light row green
- Per-wallet sequence: (1) bob signs paySkillRun popup → (2) alice signs withdrawCreator popup → (3) operator signs treasury withdraw popup

These pixel-level proofs add **product-feel** evidence on top of the contract-level proof. The contract-level proof IS sufficient to claim "the flow works"; the MM popup smoke proves "the flow feels right when a real user does it."

## Pending sub-artifacts

`mm-popups/` directory pending; will be captured by the Playwright + MM harness invocation in the next cron iteration. The harness reuses the existing pre-onboarded MM extension at `scripts/qa/metamask-e2e/mm/` so no fresh extension setup is needed.
